<% from common.conf import CollectdPluginDestDir %>\
<% from common.constants import NAME, INTERVAL, AMBARI_USERNAME, AMBARI_PASSWORD%>\
<Plugin python>
        ModulePath "${CollectdPluginDestDir}"
        LogTraces true
        Interactive false
        Import "${data[NAME]}"
         <Module ${data[NAME]}>
            % if INTERVAL in data:
                interval "${data[INTERVAL]}"
            % endif
            ambari_username "${data[AMBARI_USERNAME]}"
            ambari_password "${data[AMBARI_PASSWORD]}"
        </Module>
</Plugin>
